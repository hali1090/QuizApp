import axios from 'axios';
import { setAlert } from './alert';

import {
  GET_RESULTS,
} from './types';

// Get the current results from user feedback
export const getResults = () => async (dispatch) => {


};

// Submit a users feedback
export const sendFeedback = () => async (dispatch) => {

};